[pytest]
; Lets tests import project modules the same way the app does (e.g. "from db.model import
; Organism"), without needing an editable install or sys.path hacks in every test file.
pythonpath = .
testpaths = tests
